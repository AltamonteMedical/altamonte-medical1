function collectFormData(){const form=document.getElementById('intakePacketForm');const data={};const checkboxGroups={};form.querySelectorAll('input,select,textarea').forEach(el=>{if(!el.name)return;if(el.type==='checkbox'){checkboxGroups[el.name]=checkboxGroups[el.name]||[];if(el.checked)checkboxGroups[el.name].push(el.value);}else if(el.type==='radio'){if(el.checked)data[el.name]=el.value;}else{data[el.name]=el.value;}});Object.keys(checkboxGroups).forEach(k=>data[k]=checkboxGroups[k]);return data;}function savePacket(){localStorage.setItem('altamonteIntakePacket',JSON.stringify(collectFormData()));alert('Progress saved on this device.');}function loadPacket(){const raw=localStorage.getItem('altamonteIntakePacket');if(!raw)return;try{const data=JSON.parse(raw);const form=document.getElementById('intakePacketForm');Object.keys(data).forEach(key=>{const els=form.querySelectorAll('[name="'+key+'"]');els.forEach(el=>{if(el.type==='checkbox'&&Array.isArray(data[key]))el.checked=data[key].includes(el.value);else if(el.type==='radio')el.checked=el.value===data[key];else if(!Array.isArray(data[key]))el.value=data[key];});});}catch(e){}}function downloadJSON(){const blob=new Blob([JSON.stringify(collectFormData(),null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='altamonte-intake-packet.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500);}function printPacket(){window.print();}document.addEventListener('DOMContentLoaded',loadPacket);